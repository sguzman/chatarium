# Flows

Flow documents describe user-visible actions and the observed sequence of remote/client behavior across one or more protocol snapshots.

Examples will include send text, stop generation, retry/regenerate, open conversation, and attachment upload. No flow is documented as fact until backed by a snapshot.
