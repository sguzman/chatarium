# Snapshots

Each child directory is one timestamped protocol observation set. Snapshot IDs use `YYYY-MM-DD.NNN`.

Do not create a fake baseline from memory or implementation guesses. The first snapshot begins when controlled capture evidence exists.
