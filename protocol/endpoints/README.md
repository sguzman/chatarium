# Endpoints

This directory accumulates cross-snapshot endpoint documentation. Endpoint pages should cite the observation revisions in which behavior was seen and call out renamed, removed, optional, or unexplained fields.

Do not infer endpoint behavior solely from its URL or name.
