# Fixtures

Fixtures are sanitized protocol examples suitable for tests. Every fixture should name the snapshot from which it was derived and must contain no reusable credentials or unrelated private conversation data.
